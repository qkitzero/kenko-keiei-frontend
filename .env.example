# Public site origin (used to build the Auth0 redirect_uri and return_to)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Backend gateway base URLs (server-side only, consumed by the BFF Route Handlers)
AUTH_SERVICE_URL="http://localhost:8081"
USER_SERVICE_URL="http://localhost:8082"
